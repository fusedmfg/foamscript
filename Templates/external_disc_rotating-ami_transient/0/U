/*--------------------------------*- C++ -*----------------------------------*\
| =========                 |                                                 |
| \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox           |
|  \\    /   O peration     | Version:  v2512                                 |
|   \\  /    A nd           | Website:  www.openfoam.com                      |
|    \\/     M anipulation  |                                                 |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       volVectorField;
    object      U;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

// Hard-coded constant
dimensions      [0 1 -1 0 0 0 0];

// Parameterized: Initialize to freestream velocity (prevents adaptive timestep FPE)
internalField   uniform ({{ ux }} 0 {{ uz }});

boundaryField
{
    inlet
    {
        type            fixedValue;

        // Scriban parameters: ux, uz
        value           uniform ({{ ux }} 0 {{ uz }});
    }

    outlet
    {
        type            zeroGradient;
    }

    tunnel
    {
        type            slip;
    }

    disc
    {
        type            noSlip;
    }

    "rotor.*"
    {
        type            cyclicAMI;

        // Scriban parameters: ux, uz
        value           uniform ({{ ux }} 0 {{ uz }});
    }
}

// ************************************************************************* //
